"""
Normalizes property data to a unified schema.
"""
def normalize_data(cleaned_data):
    # Convert all records to unified schema: price, rent, location, bedrooms, property_type, etc.
    # Return normalized list of property dicts
    raise NotImplementedError("Normalization logic must be implemented.")
